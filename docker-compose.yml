services:
  docs-share:
    build: .
    container_name: docs-share
    ports:
      - "3457:3457"
    volumes:
      - ./docs:/app/docs
      - ./users.json:/app/users.json
    restart: unless-stopped
